# Post-Market Maintenance Task (2026-04-06)

The following steps are required to finalize the independence of the Google Cloud VM from the local MacBook for secret/key synchronization.

## Context
Although the latest deployment (`full_deploy.sh`) successfully installed the `fetch_secrets.sh` script and updated the `terminator.service`, the VM currently fails to access GCP Secret Manager due to **insufficient Access Scopes**.

## Steps to Execute (After 15:30 PM Chicago Time)

1. **Stop the VM**
   - Instance: `production-server`
   - Zone: `us-central1-a`
   - Reason: You cannot modify scopes while a VM is running.

2. **Update Access Scopes**
   - Command: `gcloud compute instances set-service-account production-server --scopes=https://www.googleapis.com/auth/cloud-platform --project=terminator-478221 --zone=us-central1-a`
   - Goal: This allows the VM's service account to utilize its `Secret Manager Secret Accessor` role.

3. **Start the VM**
   - Verify it boots correctly.

4. **Verify Secret Fetching**
   - Command: `journalctl -u terminator -n 100`
   - Expected Output: 
     - "Fetching Schwab API keys from Secret Manager..."
     - "Successfully fetched sli_api.json"
     - "Successfully fetched sli_token.json"
     - "Secret synchronization complete."

5. **Sanity Check**
   - Confirm the `terminator` service successfully initializes the Schwab client after the fetch.

---
**Status**: Pending Market Close
